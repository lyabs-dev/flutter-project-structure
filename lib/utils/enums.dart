
enum CustomState {
  loading,
  done,
  error,
}

enum HttpMethod {
  post,
  get,
  put,
  delete
}

enum MessageType {
  dialog,
  toast,
}

enum DialogType {error, success, info, confirm, warning}